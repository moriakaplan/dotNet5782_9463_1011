<Window x:Class="PL.DroneListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="DroneList" Height="640" Width="800">
    <Grid Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="400*"/>
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>
        <Grid Name="UpGrid" HorizontalAlignment="Stretch" Height="auto" Grid.Row="0"
VerticalAlignment="Stretch" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions >
            <Label Content="Filter By Status" HorizontalAlignment="Left" Margin="60,0,0,0" VerticalAlignment="Center"/>
            <Label Content="Filter By Max Weight" Grid.Column="2" HorizontalAlignment="Left" Margin="59,0,0,0" VerticalAlignment="Center"/>
            <ComboBox x:Name="StatusFilter" Grid.Column="1" HorizontalAlignment="Left" Margin="67,22,0,0" VerticalAlignment="Top" Width="120" SelectionChanged="StatusFilter_SelectionChanged"/>
            <ComboBox x:Name="WeightFilter" Grid.Column="3" HorizontalAlignment="Center" Margin="0,19,0,0" VerticalAlignment="Top" Width="120" SelectionChanged="WeightFilter_SelectionChanged"/>
        </Grid>
        <ListView x:Name="DroneListView" Margin="0,4,0,0" Grid.Row="1" SelectionChanged="DroneListView_SelectionChanged" MouseDoubleClick="ViewDrone">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Add new Dronw" HorizontalAlignment="Center" Grid.Row="2" VerticalAlignment="Center" 
                Click="AddDrone_Click"/>
    </Grid>
</Window>
