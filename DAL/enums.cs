using System;

namespace IDAL
{
    namespace DO
    {
        enum WeightCategories { easy, medium, heavy};
        enum Priorities { rgular, fast, emergency};
        enum DroneStatuses { vacant, maintenance, sending};
    }
}