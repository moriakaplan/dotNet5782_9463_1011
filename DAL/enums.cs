using System;

namespace IDAL
{
    namespace DO
    {
        public enum WeightCategories { easy, medium, heavy};
        public enum Priorities { regular, fast, emergency};
        public enum DroneStatuses { vacant, maintenance, sending};
    }
}